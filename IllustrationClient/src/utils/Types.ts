export type RegesitrationFields = {
    username:string,
    email:string,
    password:string,
    repeatedPassword:string
}

export type LoginFields = {
    email:string,
    password:string
}

export type UserDTO = {
    id:string,
    username:string,
    email:string,
    password:string,
}

